server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/landrzejewski/spring-boot-clean-architecture
          search-paths: config-files
